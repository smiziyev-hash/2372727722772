# Инструкция по деплою на Vercel

## ✅ Что было исправлено:

1. **Упрощен `layout.tsx`** - убраны лишние проверки, код стал проще
2. **Исправлена ошибка TypeScript** в `locale-store.ts`
3. **Все компоненты проверяют массив** перед итерацией
4. **Команда сборки**: `npm run build.server` (генерирует клиентский манифест + сервер)

## 📋 Настройки Vercel:

1. Откройте **Settings → General** на Vercel
2. Установите:
   - **Root Directory**: `web`
   - **Build Command**: `npm run build.server`
   - **Output Directory**: (пусто)
   - **Install Command**: `npm install --legacy-peer-deps`
   - **Framework Preset**: `Other`

3. **ВАЖНО**: После изменения настроек нажмите **"Redeploy"** в разделе General

## 🔗 Файл данных:

Данные загружаются из:
`https://raw.githubusercontent.com/inktech-sc/Digital_security_web/main/checklist-data.yml`

Убедитесь, что файл `checklist-data.yml` существует в репозитории.

## ✅ Готово к деплою!

После настройки Vercel автоматически пересоберет проект с новыми настройками.

